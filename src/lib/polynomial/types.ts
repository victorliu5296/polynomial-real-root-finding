/**
 * A Coefficient is a real number.
 */
export type Coefficient = number;
/**
 * A Polynomial is an array of coefficients, where the coefficients are in increasing order of degree.
 */
export type Polynomial = Coefficient[];